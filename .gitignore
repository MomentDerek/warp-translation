# Build artifacts
build/
reports/
tools/target/

# OS / editor
.DS_Store
*.swp
*.swo

# Runtime metadata (per research/translation-table-format.md)
translations/.lock.json

# Claude Code runtime lock (PID + session id)
.claude/scheduled_tasks.lock
